Implement check mode
Return txid after making a transaction
Allow user to specify server and port
Implement getnewaddress
Implement getbalance (confirmed and unconfirmed)
Wait for n confirmations before exit, default=0
Implement wait until payment is done
